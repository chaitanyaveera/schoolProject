import { NumberdirectivesDirective } from './numberdirectives.directive';

describe('NumberdirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberdirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
